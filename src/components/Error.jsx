import { Alert } from "react-bootstrap"

const Error = () => {

  return (
    <Alert variant="danger mt-3">Error: something went wrong! the fetch call isn't write properly...</Alert>
  )
}

export default Error